all:
  hosts:
    localvm-tunnel:
      ansible_user: oni
      ansible_host: 10.0.0.211
      ansible_ssh_private_key_file: /root/.ssh/semaphore_key
      # ansible_port: 22  # Uncomment if SSH runs on a different port
